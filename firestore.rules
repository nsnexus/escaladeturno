rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita para a escala operacional
    match /escala_operacional/{document=**} {
      allow read, write: if true;
    }
  }
}
